{
	"role":{
		"name": "osr.edw.data.info::read_user",
		"schema_privileges": [{
		   "privileges": ["SELECT METADATA",
		    			  "SELECT CDS METADATA",
		    			  "SELECT",
		    			  "EXECUTE",
		    			  "DEBUG"
		  ]
		}]
     }
}